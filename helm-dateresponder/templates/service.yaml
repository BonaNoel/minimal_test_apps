apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-dateresponder.fullname" . }}
  labels:
    {{- include "helm-dateresponder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-dateresponder.selectorLabels" . | nindent 4 }}
