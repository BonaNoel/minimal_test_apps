apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-datecaller.fullname" . }}
  labels:
    {{- include "helm-datecaller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-datecaller.selectorLabels" . | nindent 4 }}
